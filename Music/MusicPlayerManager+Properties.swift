import Foundation
import Combine

// TEMP COMPILE STUB - removed all conflicting stored properties and duplicate methods
// Original extension had stored properties (not allowed in Swift extensions)
// and duplicate RepeatMode enum + method redeclarations
extension MusicPlayerManager {
    // Extension intentionally minimal to avoid conflicts with main class
}
